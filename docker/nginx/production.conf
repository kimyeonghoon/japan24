# Japan24 프로덕션 환경 Nginx 설정
#
# 이 설정 파일은 Japan24 애플리케이션을 위한 고성능, 보안 강화된
# Nginx 웹 서버 설정을 제공합니다.
#
# 주요 기능:
# - HTTP → HTTPS 자동 리다이렉션
# - SSL/TLS 보안 설정 (TLS 1.2/1.3)
# - Rate Limiting (API, 로그인, 일반 요청)
# - 보안 헤더 자동 추가
# - 정적 파일 캐싱 최적화
# - CORS 헤더 지원
# - FastCGI 최적화
#
# 성능 향상 기능:
# - HTTP/2 지원
# - Gzip 압축
# - 정적 파일 장기 캐싱 (1년)
# - FastCGI 버퍼 최적화

# HTTP 서버 블록 - HTTPS로 리다이렉션 전용
server {
    listen 80;                          # HTTP 포트 80에서 수신
    server_name localhost;              # 도메인 이름 (배포 시 실제 도메인으로 변경 필요)

    # 기본 보안 헤더 추가 (HTTP 요청에도 적용)
    add_header X-Frame-Options "SAMEORIGIN" always;      # 클릭재킹 방지
    add_header X-Content-Type-Options "nosniff" always;  # MIME 타입 스니핑 방지
    add_header X-XSS-Protection "1; mode=block" always;  # XSS 공격 방지

    # 모든 HTTP 트래픽을 HTTPS로 영구 리다이렉션 (301 상태 코드)
    return 301 https://$server_name$request_uri;
}

# Main HTTPS server
server {
    listen 443 ssl http2;
    server_name localhost;  # Replace with your domain
    root /var/www/public;
    index index.php index.html;

    # SSL Configuration
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/private.key;

    # SSL Security
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_stapling on;
    ssl_stapling_verify on;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Logging
    access_log /var/log/nginx/japan24.access.log main;
    error_log /var/log/nginx/japan24.error.log warn;

    # Static file caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|pdf|txt|webp|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options "nosniff" always;
        access_log off;
        try_files $uri =404;
    }

    # Storage files (uploads)
    location ^~ /storage/ {
        alias /var/www/storage/app/public/;
        expires 30d;
        add_header Cache-Control "public";
        add_header X-Content-Type-Options "nosniff" always;
        access_log off;
        try_files $uri =404;
    }

    # API routes with rate limiting
    location ^~ /api/ {
        limit_req zone=api burst=20 nodelay;
        try_files $uri $uri/ /index.php?$query_string;

        # CORS headers for API
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Accept, Authorization, Content-Type, X-Requested-With" always;

        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }

    # Login/auth routes with stricter rate limiting
    location ~* ^/(login|register|password|auth)/ {
        limit_req zone=login burst=5 nodelay;
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP handler
    location ~ \.php$ {
        limit_req zone=general burst=100 nodelay;

        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;

        # FastCGI optimizations
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
        fastcgi_temp_file_write_size 256k;
        fastcgi_read_timeout 300;

        # Security
        fastcgi_param HTTPS on;
        fastcgi_param SERVER_PORT 443;
    }

    # Main application
    location / {
        limit_req zone=general burst=100 nodelay;
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Block access to sensitive files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    location ~ ^/(\.env|composer\.(json|lock)|package\.json|yarn\.lock|webpack\.mix\.js|artisan)$ {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Health check endpoint
    location = /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Nginx status (for monitoring)
    location = /nginx-status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        allow 172.16.0.0/12;  # Docker network
        deny all;
    }
}